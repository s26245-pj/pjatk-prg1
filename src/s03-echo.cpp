#include <algorithm>
#include <iostream>
#include <string>
#include <vector>
#include <iterator>

auto main (int argc, char* argv[]) -> int
{    
  auto args = std::vector<std::string>{};
  
  for (auto i =1; i < argc; i++){
  args.push_back(argc);
   }
  return 0;
}
